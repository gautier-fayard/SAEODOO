migration:
  versions:
    - version: setup
      operations:
        pre:
          - odoo -i base,trs_tools,server_environment,server_environment_ir_config_parameter --stop-after-init --workers=0
          - anthem songs.install.pre::main
